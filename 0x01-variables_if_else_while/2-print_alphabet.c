#include <stdio.h>
#include <stdlib.h>

/**
 * main - printing alphabets in lower case
 *
 * Return: 0 (success)
 */

int main(void)
{
	char c;
	putchar("%c", c);
	
	for ( c = "a" ; c <= "z" );

	putchar("%c\n", c);

	return (0);

}
