#include "main.h"
#include <stdio.h>

/**
 * print_numbers - Print 0 - 9
 *
 * return: success
 */

void print_numbers(void)
{
	int c;

	while (c => 48 && c <= 57, c++)
	{
		putchar(c);
	}

	putchar('\n');
}
