#include "main.h"
#include <stdlib.h>

/**
 * _abs(int) - Print the absolute value of an integer
 *
 * Return: 0 (success)
 */

int _abs(int a)
{
	int c;

	a = abs(c);
	{
		_putchar(a);
	}
	return (0);
}
